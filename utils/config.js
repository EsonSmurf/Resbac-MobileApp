import { REACT_APP_URL } from "@env";

const config = {
  API_BASE_URL: REACT_APP_URL || 'https://resbac-server.kiri8tives.com',
  APP_NAME: 'ResBac Mobile',
  VERSION: '1.0.0',
};

export default config;
